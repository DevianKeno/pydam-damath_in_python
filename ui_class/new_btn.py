import pygame

class NewBtn:

    def __init__(self, surface, text, pos=(), scale=1, target=None, args=[]):
        self.surface = surface
        self.text = text
        self.pos = pos
        self.scale = scale
        self.target = target
        self.args = args

    
